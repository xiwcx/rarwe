<ul class="rr-list">
  {{#each model.songs as |song|}}
    <li class="rr-list-item">
      {{song.title}} <span class="rr-rating fr">{{song.rating}}</span>
    </li>
  {{else}}
    <li class="rr-empty-message">
      No {{model.name}} songs yet.
    </li>
  {{/each}}
</ul>
