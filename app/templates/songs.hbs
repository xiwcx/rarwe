<ul class="list pl3">
    {{#each model as |song|}}
        <li>
            {{song.title}} by <em>{{song.band}}</em>
            <span class="rr-rating fr">{{song.rating}}</span>
        </li>
    {{/each}}
</ul>
